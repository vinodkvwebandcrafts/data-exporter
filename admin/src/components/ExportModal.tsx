// Placeholder; the real implementation lands in Task 15.
// This stub exists so that ExportButton (Task 14) can import the symbol
// without breaking `tsc --noEmit`.
import * as React from "react";

type Props = { uid: string; onClose: () => void };

export const ExportModal = (_props: Props): React.ReactElement | null => null;
